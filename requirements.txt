# Runtime dependencies
Flask>=3.0,<4.0
pystray>=0.19,<0.26
Pillow>=10.0

# Dev / packaging / testing (NOT runtime):
# pyinstaller   - only needed to build the standalone exe (build.spec)
# requests      - only needed to write external API tests
