# adb-to-http-api
This service (ADB HTTP API Server) wraps the local adb (Android Debug Bridge) into a set of HTTP endpoints, enabling local scripts, GUIs, or automation agents to control connected Android devices via HTTP requests—without having to manually assemble subprocess calls, handle device selection, or parse command output.
